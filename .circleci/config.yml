version: 2

 jobs:
   build:
     ...
   deploy:
     docker:
       - image: <docker-image-name-tag>
     steps:
       - checkout
       - run:
           name: Deploy Master to Heroku
           command: |
             git push https://heroku:$c69bc344-2b68-4b25-8929-666e8bd93e01@git.heroku.com/$circlecitest1.git master
 workflows:
   version: 2
   build-deploy:
     jobs:
       - build
       - deploy:
           requires:
             - build # only run deploy-via-git job if the build job has completed
           filters:
             branches:
               only: master # only run deploy-via-git job on master branch
